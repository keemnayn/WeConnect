<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.arezip.weconnect.mapper.MemberMapper">
<insert id="register" parameterType="com.arezip.weconnect.model.dto.MemberDTO">
 INSERT INTO member (member_id, member_name, member_email, member_password, position,department_id)
  VALUES (member_seq.NEXTVAL, #{memberName}, #{memberEmail}, #{memberPassword}, #{position},#{departmentVO.departmentId})
</insert>
	<select id="findByDepartMentNAME" resultType="com.arezip.weconnect.model.dto.DepartmentDTO">
	select * from department
	</select>
	<select id="login" resultType="com.arezip.weconnect.model.dto.MemberDTO">
	SELECT member_name from member WHERE member_email= #{memberEmail} AND member_password=#{memberPassword}
	</select>
	<select id="findByEmail" resultType="com.arezip.weconnect.model.dto.MemberDTO">
	select * from member where member_email =#{memberEmail}
	</select>
</mapper>